name: 仙加味 n8n 晚間預熱

on:
  schedule:
    # 台灣時間 19:03～00:53，每 10 分鐘預熱一次。
    # GitHub Actions cron 使用 UTC，因此為 11:03～16:53 UTC。
    # 2026-09-11 將分鐘位移，重新註冊正式 schedule，避免先前排程長時間未觸發。
    - cron: '3,13,23,33,43,53 11-16 * * *'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: xianjiawei-n8n-evening-warm
  cancel-in-progress: true

jobs:
  warm-n8n:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: 晚間預熱 n8n longterm
        shell: bash
        run: |
          set -euo pipefail
          URL='https://xianjiawei-n8n-longterm.onrender.com/healthz'
          for attempt in 1 2 3; do
            if curl --fail --silent --show-error --location \
              --connect-timeout 20 --max-time 120 \
              "$URL" >/dev/null; then
              echo 'n8n longterm 預熱完成'
              exit 0
            fi
            echo "第 ${attempt} 次預熱未完成，15 秒後重試"
            sleep 15
          done
          echo 'n8n longterm 預熱失敗'
          exit 1
