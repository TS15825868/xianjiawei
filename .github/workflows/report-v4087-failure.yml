name: Report v408.7 retry exact failure

on:
  issues:
    types: [opened]

permissions:
  actions: read
  issues: write

jobs:
  report:
    if: github.event.issue.title == 'REPORT_V4087_RETRY_EXACT'
    runs-on: ubuntu-latest
    steps:
      - name: Collect exact failed run
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set +e
          gh run view 29244139898 --repo "${{ github.repository }}" --log-failed > /tmp/failed.log 2>&1
          {
            echo 'V4087_RETRY_EXACT_REPORT'
            echo '```text'
            tail -140 /tmp/failed.log
            echo '```'
          } > /tmp/comment.txt
          gh issue comment "${{ github.event.issue.number }}" --repo "${{ github.repository }}" --body-file /tmp/comment.txt
          gh issue close "${{ github.event.issue.number }}" --repo "${{ github.repository }}" --reason completed
