name: Apply site v408.7 retry

on:
  issues:
    types: [opened]

permissions:
  contents: write
  issues: write
  actions: read

concurrency:
  group: apply-site-v4087-retry
  cancel-in-progress: false

jobs:
  apply:
    if: github.event.issue.title == 'APPLY_SITE_V4087_RETRY'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout latest main
        uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0

      - name: Download verified scripts
        run: |
          set -euo pipefail
          python3 -m pip install --quiet gdown beautifulsoup4 pillow
          gdown '1JFafCAXXmcpCpf15nKTqi-dmC9-dtZk4' -O /tmp/apply_v4087.py
          gdown '1EUK87BwJdKK-oXZ03TVdMBQgGWQeK16S' -O /tmp/deploy-pages-v4087.yml
          echo '3d1736577a3ff8a75141e0d2316af7a266ea97391b871c01520963bb739a1fef  /tmp/apply_v4087.py' | sha256sum -c -
          echo 'd99bbd29f89ac0e8f083086d6ab8f9fac15c193f6f403ca2051dbf4b8f449193  /tmp/deploy-pages-v4087.yml' | sha256sum -c -

      - name: Apply v408.7 and install deploy workflow
        run: |
          set -euo pipefail
          python3 /tmp/apply_v4087.py
          cp /tmp/deploy-pages-v4087.yml .github/workflows/deploy-pages.yml
          rm -f .github/workflows/apply-site-v4087-full.yml
          rm -f .github/workflows/apply-site-v4087-retry.yml
          rm -f .github/workflows/report-v4087-failure.yml
          rm -f .github/workflows/report-deploy-v4086.yml
          rm -f .github/workflows/report-deploy-v4086-log.yml
          rm -f .github/workflows/report-deploy-v4086-error.yml
          rm -f .github/workflows/confirm-deploy-v4086-success.yml
          rm -f .github/workflows/confirm-latest-deploy-v4086.yml
          rm -f .github/workflows/report-dm-v4086-failure.yml

      - name: Commit, rebase and push main
        id: commit
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m '完成整站SEO AIO產品頁與效能優化 v408.7'
          for i in 1 2 3; do
            git fetch origin main
            git rebase origin/main
            if git push origin HEAD:main; then break; fi
            [ "$i" = 3 ] && exit 1
            sleep 3
          done
          echo "sha=$(git rev-parse HEAD)" >> "$GITHUB_OUTPUT"

      - name: Wait for successful Pages deployment
        env:
          GH_TOKEN: ${{ github.token }}
          TARGET_SHA: ${{ steps.commit.outputs.sha }}
        run: |
          set -euo pipefail
          echo '[]' > /tmp/runs.json
          for i in $(seq 1 42); do
            gh run list --repo "${{ github.repository }}" --workflow deploy-pages.yml --commit "$TARGET_SHA" --limit 5 --json databaseId,status,conclusion,headSha,displayTitle,createdAt > /tmp/runs.json || true
            status=$(python3 - <<'PY'
          import json
          try:
              runs=json.load(open('/tmp/runs.json'))
              print(runs[0].get('status','') if runs else '')
          except Exception:
              print('')
          PY
            )
            [ "$status" = 'completed' ] && break
            sleep 10
          done
          conclusion=$(python3 - <<'PY'
          import json
          runs=json.load(open('/tmp/runs.json'))
          print(runs[0].get('conclusion','') if runs else '')
          PY
          )
          cat /tmp/runs.json
          test "$conclusion" = 'success'

      - name: Report success
        if: success()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh issue comment "${{ github.event.issue.number }}" --repo "${{ github.repository }}" --body 'SITE_V4087_SUCCESS｜首頁、產品總覽、六項完整產品頁、正式DM、SEO/AIO、sitemap、robots、llms、機器可讀產品資料、WebP圖片與精簡部署全部驗收並正式上線。'
          gh issue close "${{ github.event.issue.number }}" --repo "${{ github.repository }}" --reason completed

      - name: Report failure
        if: failure()
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh issue comment "${{ github.event.issue.number }}" --repo "${{ github.repository }}" --body 'SITE_V4087_RETRY_FAILURE｜重試流程未完成，不宣稱上線；請查看失敗步驟。'
