name: 官網試喝與出貨舊規則入口

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  delegated:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo '此入口已停用自動寫回。'
          echo '官網、LINE OA、ERP 的正式資料只由單一三端母本更新，避免5～7工作天文字反覆互相覆蓋。'
