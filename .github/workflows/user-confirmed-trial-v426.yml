name: 官網使用者確認試喝規則守門 v426

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  delegated:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'v426 已由 v427 三端統一規則取代。'
          echo '正式規則：製作加工約需5～7個工作天；完成後才安排出貨，物流配送時間另計。'
          echo '停止排程與自動寫回，避免再次覆蓋官網、LINE OA 與 ERP 的共同基準。'
